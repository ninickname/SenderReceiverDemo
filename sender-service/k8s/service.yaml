apiVersion: v1
kind: Service
metadata:
  name: sender-service
  labels:
    app: sender-service
spec:
  selector:
    app: sender-service
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP