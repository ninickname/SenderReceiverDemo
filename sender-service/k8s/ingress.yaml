apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sender-service-ingress
  labels:
    app: sender-service
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: sender-service.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sender-service
            port:
              number: 80